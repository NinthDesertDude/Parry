namespace Parry
{
    public class Bundler
    {
        /*
            TODO: Create a format for (de)hydrating data from Character or
            Session. On dehydration, store a GUID in place of all functions.
            On hydration, take a function-to-GUID map and bind functions with
            matching GUIDs. Have an overload that skips the re-binding process
            in case the client wants to do it themselves.
        */
    }
}
